/**
 * Angular animation timings mirror the CSS Product Motion tokens. Angular's
 * animation parser does not resolve CSS custom properties at runtime.
 */
export const PLP_ANGULAR_MOTION = {
  sidebar: '160ms cubic-bezier(0.2, 0.8, 0.2, 1)'
} as const;
